# @llmintel/cli

Public mirror of the [`@llmintel/cli`](https://www.npmjs.com/package/@llmintel/cli) package.

This repository is generated: its source (under `packages/`) is mirrored from a private monorepo on
each release, and this repo publishes the package to npm **with provenance**. See the package README
under [`packages/cli`](./packages/cli) for usage.

Contents will be populated by the first mirrored release.
